name: CI

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Build src
        run: |
          $env:Platform = ""
          dotnet build src/ --configuration Release --no-incremental
        shell: pwsh

      - name: Build tests
        run: |
          $env:Platform = ""
          dotnet build tests/ --configuration Release --no-incremental
        shell: pwsh

      - name: Run tests
        run: |
          $env:Platform = ""
          dotnet test tests/ --configuration Release --no-build --verbosity normal
        shell: pwsh

  publish:
    needs: build-and-test
    runs-on: windows-latest
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Pack
        run: |
          $env:Platform = ""
          dotnet pack src/ --configuration Release --no-incremental --output ./nupkgs
        shell: pwsh

      - name: Publish to NuGet.org
        run: |
          dotnet nuget push ./nupkgs/*.nupkg --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        shell: pwsh
